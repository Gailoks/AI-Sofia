import torch
import torch.nn as nn
import ServiceTokens as st
import torch.nn.functional as F

device = 'cpu'

token = torch.LongTensor([[1,2,3,4,5,6,7],[7,5,4,3,2,1,6]])
#print(token,token.shape)
for micro_token in token:
    print(micro_token,micro_token.shape)
    break